#include"main.h"

/**
 *  * main - Prints the alphabetic
 *   *
 *    * Return: Always (Success)
 *     */

void print_alphabet(void)
{
		int ch;

			for (ch = 'a'; ch <= 'z'; ++ch)
						_putchar(ch);
				_putchar('\n');
}

